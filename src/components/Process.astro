---
import TopoBackground from './TopoBackground.astro';

const steps = [
  { n: '01', title: 'Book your shoot', body: 'Choose a package and schedule online. Same-week availability in most markets.' },
  { n: '02', title: 'We come to you', body: 'Our photographer arrives on time, fully equipped. No prep needed on your end.' },
  { n: '03', title: 'Editing & delivery', body: 'Professional post-processing overnight. Your gallery is ready within 48 hours.' },
  { n: '04', title: 'List with confidence', body: 'Download your assets and publish. Every file is print-ready and MLS-optimized.' },
];
---

<section id="process" class="process">
  <TopoBackground />
  <div class="container process-inner">
    <div class="process-header reveal">
      <span class="section-label">How It Works</span>
      <h2 class="section-heading light">From booking to listing<br />in 48 hours.</h2>
    </div>
    <ol class="steps">
      {steps.map((s, i) => (
        <li class="step reveal" style={`transition-delay: ${i * 100}ms`}>
          <div class="step-top">
            <span class="step-num">{s.n}</span>
            <span class="step-rule" aria-hidden="true"></span>
          </div>
          <div>
            <h3 class="step-title">{s.title}</h3>
            <p class="step-body">{s.body}</p>
          </div>
        </li>
      ))}
    </ol>
  </div>
</section>

<style>
  .process {
    position: relative;
    background: var(--color-obsidian);
    padding-top: 0;
    padding-bottom: var(--section-y);
    overflow: hidden;
  }

  .process-inner {
    position: relative;
    z-index: 2;
    padding-top: var(--section-y);
  }

  .process-header {
    margin-bottom: var(--space-12);
  }

  .steps {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-8);
  }

  .step {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .step-top {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-2);
  }

  .step-num {
    font-family: var(--font-display);
    font-size: 40px;
    font-weight: 300;
    color: var(--color-gold);
    opacity: 0.65;
    line-height: 1;
    flex-shrink: 0;
  }

  .step-rule {
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, rgba(201,168,76,0.25), transparent);
  }

  .step-title {
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.04em;
    color: var(--color-warm-white);
    margin-bottom: var(--space-2);
  }

  .step-body {
    font-size: 13px;
    font-weight: 300;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.45);
  }

  @media (max-width: 900px) {
    .steps { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 560px) {
    .steps { grid-template-columns: 1fr; }
  }
</style>
